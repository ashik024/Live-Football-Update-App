<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.footballlive">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LeagueTableFrance" />
        <activity android:name=".LeagueTableGermany" />
        <activity android:name=".LeagueTableItaly" />
        <activity android:name=".LeagueTablesSpain" />
        <activity android:name=".Higlights" />
        <activity android:name=".LeagueTable" />
        <activity android:name=".UpcomingFixture" />
        <activity android:name=".detaileScore" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>